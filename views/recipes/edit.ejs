<% include ../styling/header %>

<div class="container">
    <div class="row">
        <div style="width:30%; margin: 30px auto;">
                <h1 class="h3 mb-3 font-weight-normal">Edit your Recipe</h1>
            <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <input class="form-control" required type="text" name="recipe[name]" value="<%= recipe.name %>">
                </div>
                <div class="form-group">
                    <label for="image">Image</label>
                    <input type="file" id="image" required name="image" accept="image/*">
                </div>
                <div class="form-group">
                    <textarea style="width: 100%" name="recipe[ingredients]" required
                       ><%= recipe.ingredients %>
					</textarea>
                </div>
                <div class="form-group">
                    <textarea style="width: 100%" name="recipe[description]" required
                    ><%= recipe.description %>
                    </textarea>
                </div>
                <!--  -->
                <div class="form-group">
                    <button class="btn btn-info btn-block">Update</button>
                </div>
            </form>
          
        </div>
    </div>
</div>
</body>
</html>